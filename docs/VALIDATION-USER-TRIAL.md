# 首轮用户试用（本机 Docker）

> 2026-09-18：用户先试用，其他试用者稍后安排。实际模型固定为 `deepseek-flash`（用户已确认）。尚未获得体感反馈，不计为通过。

## 入口与限制

入口：<http://127.0.0.1:18791>。访问令牌由当前会话提供；不要填写 DeepSeek API 密钥。令牌只保存在当前标签页，重新打开页面可能需要重新输入。

本轮可以关闭浏览器页面，但运行服务的电脑和 Docker 必须保持在线，不能关机或休眠。入口仅本机可用，不代表已验证云主机离场或手机取回。仅使用下方自有公开仓库，不提交敏感信息，不尝试生产部署、push 或 PR。

## 约 10 分钟试用

先自行探索页面，不需要查看终端或事件日志。记录卡住的地方即可，不必替系统排障。

1. 输入访问令牌，进入工作台。
2. 填写以下任务，最长时间设为 3 分钟，最大轮次保留 16，分支留空。
3. 提交后，判断你是否放心关闭页面；记下理由。关闭页面，约一分钟后重新打开并找到自己的任务。
4. 不看底层事件，判断任务是否完成、改了什么、测试是否执行、哪些尚未验证。下载 patch。
5. 可选失败场景：保持仓库不变，将 commit 改为 40 个 `0`，目标写“调查 package.json 的 Node 版本要求”，验收条件写“说明 Node 版本要求”。观察能否理解失败原因与下一步。

仓库 URL：

```text
https://github.com/weank1984/vcoder-bot.git
```

完整 commit：

```text
5e2bc4e535b93e5ca792606c62d266c732fb8adc
```

目标：

```text
创建 docs/agent-cloud-smoke.md。
第一行必须是 # Agent cloud validation。
正文必须包含输入 commit 的完整 SHA，以及一句 No remote side effects were requested.。
除该文件外不要修改任何文件；不要 push 或创建 PR。
完成后运行 git diff --check，并报告退出码。
```

验收条件（每行一项）：

```text
仅创建 docs/agent-cloud-smoke.md
标题、完整 commit SHA 和固定英文句子均存在
git diff --check 退出码为 0
提供可下载 patch，明确说明未做的验证
```

## 反馈记录（由试用者填写）

| 项目 | 记录 |
| --- | --- |
| 试用日期、任务 ID | 待填写 |
| 首次提交是否独立完成；卡在哪里 | 待填写 |
| 什么时候放心离开；哪里不确定 | 待填写 |
| 重新打开后是否找得到任务 | 待填写 |
| 是否需要看日志或求助；次数 | 待填写 |
| 是否理解测试已执行/未执行的区别 | 待填写 |
| 交付是否足以验收；缺什么 | 待填写 |
| 下次是否愿意这样委派；原因 | 待填写 |
| 最希望先改的一个问题 | 待填写 |

技术链路通过不代替用户体感通过。首位用户反馈单独记录，不能外推为 2—3 位试用者验证完成。
